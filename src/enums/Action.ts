/**
 * Enum for CRUD actions
 * @readonly
 * @enum { string }
 */
enum Action {
    CREATE = 'create',
    VIEW = 'view',
    READ = 'view',
    EDIT = 'edit',
    UPDATE = 'edit',
    DELETE = 'delete'
}

export default Action

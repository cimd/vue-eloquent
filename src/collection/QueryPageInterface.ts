export interface IQueryPage {
    current_page?: number,
    per_page?: number,
    total_pages?: number,
    first_page?: string,
    last_page?: string,
    prev_page_url?: string,
    next_page_url?: string,
}
